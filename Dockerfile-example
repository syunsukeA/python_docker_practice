#  ベースイメージの取得 ( 今回はubuntuで固定とし、ここは変更しない。 )
FROM ubuntu:latest

# ↓↓↓↓↓取得したいライブラリを指定↓↓↓↓↓
RUN < ここを変更 >

# ↓↓↓↓↓ローカルPCのファイルをコンテナにコピー (別の方法でやってもOK) ↓↓↓↓↓
COPY < ここを変更 >

# ↓↓↓↓↓デフォルトの動作ディレクトリを指定↓↓↓↓↓
WORKDIR < ここを変更 >

# ↓↓↓↓↓実行するコマンドを指定↓↓↓↓↓  [ヒント] 第一要素: コマンド, 第二要素以降: オプションor引数
CMD [ < ここを変更 > ]




